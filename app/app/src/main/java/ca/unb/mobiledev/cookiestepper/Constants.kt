package ca.unb.mobiledev.cookiestepper

object Constants {
    // Strings will serve as keys when saving state between activities
    const val ACTION_STEPS_UPDATE = "ca.unb.mobiledev.cookiestepper.STEPS_UPDATE"
    const val EXTRA_STEPS_COUNT = "ca.unb.mobiledev.cookiestepper.STEPS_COUNT"
}